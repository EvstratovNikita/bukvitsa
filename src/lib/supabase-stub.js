// Заглушка вместо @supabase/supabase-js в сборке для Игр Яндекса.
//
// На площадке Supabase не используется вообще: переменные в .env.yandex
// пустые, поэтому isSupabaseConfigured=false и клиент даже не создаётся
// (см. lib/supabase.js), анонимный вход отключён в useAuth, экономические
// RPC — в economy.js, синхронизация — в App.jsx. Но импорт статический, и
// сборщик всё равно тянул в архив весь клиент: auth, realtime, postgrest,
// storage — 245 КБ в распакованном виде.
//
// Подменяется алиасом в vite.config.js только при mode === 'yandex'.
// Веб-версии это не касается — там нужен настоящий клиент.
export const createClient = () => {
  // Недостижимо: при пустых переменных lib/supabase.js сюда не заходит, а
  // непустые не пройдут проверку архива в scripts/build-yandex.mjs (ключи в
  // сборку для площадки класть нельзя). Если всё же сработало — значит,
  // Supabase понадобился на Яндексе, и алиас надо снимать осознанно.
  throw new Error(
    'supabase-stub: на Играх Яндекса клиент Supabase недоступен — снимите алиас в vite.config.js'
  );
};
