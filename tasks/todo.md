# Буклица — что сделано 27–28 августа 2026

Сессия шла в отдельном чате Claude Code. Другой чат её переписку не видит,
поэтому здесь итог: чтобы подхватить работу, достаточно этого файла и
`git log --oneline f399ea8..HEAD` (сообщения коммитов подробные, с причинами).

Всё в `main`, коммиты `e5cfe82`…`a2c768d`, запушено в `origin/main`.

## Синхронизация косметики (веб, Supabase)

- [x] `e5cfe82` — прямой upsert в `user_stats` падал с 42501 после анти-чит-перевода;
      косметика (фон, стиль клеток, prefs) уезжает через SECURITY DEFINER
      функцию `save_cosmetics`. SQL — `supabase/save_cosmetics.sql`,
      **в проекте Supabase уже применён** (проект `bukvitsa`, ветка main).
- [x] `c0a9c64` — на чистой установке косметика берётся с сервера, а не затирается
      дефолтами; `lastSavedRef` считается от серверной строки.

## Сборка для Игр Яндекса

- [x] `e66bbe6` — шрифты вшиты в архив (`npm run fonts`), Supabase выключен
      (`--mode yandex` + `.env.yandex`), `npm run build:yandex` собирает zip
      и сам проверяет: index.html в корне, нет Google Fonts, нет ключей, SDK через `/sdk.js`.

## Потеря прогресса на площадке (главное)

Симптомы шли волнами, причин оказалось три — все закрыты:

- [x] `cea4d10` — вход в аккаунт делал `cloudSave(stats)` поверх аккаунта.
      Теперь `utils/mergeProgress.js`: счётчики по максимуму, рекорды по минимуму,
      коллекции объединением, суточные лимиты по свежему дню.
- [x] `41dcb9e` — читали у одного игрока, писали другому (вход мимо игры
      переключает игрока за нашей спиной). Личность спрашивается перед каждой
      записью; при расхождении — слияние, а не запись поверх.
- [x] `02482b6` — неудачное чтение (`getPlayer timeout`) включало сохранения, и
      снимок из дефолтов затирал аккаунт. `cloudLoad` теперь возвращает
      `{ ok, data }`; без успешного чтения запись не включается.
- [x] `d262dd8` — доска ездит в облако (ключ `__board`), первый `getPlayer` ждёт 20 с,
      чтение повторяется до успеха.

## Энергия

- [x] `b5b1c18` — при слиянии энергия сравнивается ПОСЛЕ восстановления «на сейчас»
      (раньше вход стоил единицу из-за старого якоря).
- [x] `b5b1c18` — кнопка после Слова дня называется «Новая игра» и показывает цену
      (1 ⚡ для режима на 5 букв), а не «К играм».
- [x] **Решено оставить:** на первом заходе дня, когда Слово дня сыграно и партии нет,
      игра сама начинает раунд за 1 ⚡. Убирается заменой автостарта на экран с
      кнопкой «Начать партию · 1 ⚡» — владелец согласился оставить как есть.

## Интерфейс

- [x] `a4c7e76` — цветные плитки у пунктов меню (`--mi`), градиент у «Магазина» убран,
      у «Достижений» своя иконка-звезда.
- [x] `ae90606`, `77c78c0`, `ece0cc4` — «Лидерборд» → «Лучшие игроки»: колонки
      Место / Игрок / Слов, медали у тройки, бейдж «вы», пустое состояние, кэш
      против моргания. Кнопка «Войти» — подсветка вместо заливки.
- [x] `a055e58`, `ece0cc4` — тёмная подложка под яйцом (кремовое яйцо сливалось
      со светлой), убрана кнопка сброса статистики.
- [x] `2da14d8` — панель диагностики из настроек убрана; объект остался как
      `window.__buklitsaCloud` (читается из консоли).

## Открытые хвосты

- Достижение `change_bg` в `recompute_achievements` считается по `active_background`,
  а `save_cosmetics` владение фоном не проверяет → 5 монет крафтом вызова.
  Чинить проверкой по `inventory` нельзя: два фона выдаются подарками Букли и
  лежат в `prefs.petGifts`. Решено не трогать.
- `openAuthDialog` не открывается в черновике площадки — ограничение Яндекса,
  в опубликованной версии работает.
- Со стороны консоли разработчика: лидерборд `wins` создан; остаются RTB-блоки,
  карточка игры, иконка/обложка/скриншоты (заготовка — `marketing/yandex-screens.html`).
- Дефект дев-сервера: при перезаписи файла целиком Vite иногда кэширует пустой
  модуль («does not provide an export named …») — лечится пересохранением файла.

---

# Общая таблица лидеров для VK — план (9 сентября 2026)

## Зачем

Нативный `VKWebAppShowLeaderBoardBox` сравнивает игрока только с друзьями,
у которых игра установлена. Общего рейтинга VK не даёт вовсе — у игр, где он
есть, таблица своя, на своём сервере. В описании карточки уже обещано
«соревнуйся с другими игроками», значит таблица должна быть настоящей.

## Решение

Сервер — уже имеющийся Supabase, тем же способом, что и экономика: таблица
закрыта RLS без политик, наружу торчат две `SECURITY DEFINER` функции.
Клиент площадки ходит в них голым `fetch` (PostgREST), без `@supabase/supabase-js`,
чтобы архив не потяжелел на 245 КБ.

Подпись VK проверяется на сервере (иначе счёт может прислать кто угодно за
кого угодно): параметры `vk_*` по алфавиту, HMAC-SHA256 на защищённом ключе
приложения, base64 → url-safe → без `=`. Алгоритм — из официального примера
VKCOM/vk-apps-launch-params, реализуется на `pgcrypto`.

## Шаги

- [x] `supabase/vk_leaderboard.sql`: таблица `leaderboard(platform, player_id,
      name, score, updated_at)`, PK `(platform, player_id)`, RLS без политик;
      приватная `private.app_secrets` для защищённого ключа
- [x] `submit_vk_score(p_query text, p_score int, p_name text)` — проверка
      подписи, счёт только растёт, потолок прироста и не чаще раза в 30 с
- [x] `leaderboard_top(p_platform, p_player_id)` — топ-20 + место игрока,
      в форме ответа Яндекса (`entries[].player.publicName`, `userRank`)
- [x] Самопроверка в конце файла: фикстура с тестовым ключом печатает ok/fail
- [x] `src/lib/scores.js` — fetch к PostgREST, `VITE_SCORES_URL`/`VITE_SCORES_KEY`
- [x] `src/lib/leaderboard.js` — общий фасад: Яндекс → SDK, VK → наш сервер
- [x] `Leaderboard.jsx` и `useGame.js:283` переводятся на фасад
- [x] `App.jsx` — на VK открывается наша таблица; нативная остаётся кнопкой
      «Среди друзей» внутри модалки
- [x] Имя игрока — `VKWebAppGetUserInfo`, храним имя и первую букву фамилии
- [x] `.env.vk` — адрес и публикуемый ключ; секрет приложения только в Supabase

## Что делает автор сам

Вставляет SQL в SQL Editor и одной строкой кладёт туда защищённый ключ
приложения VK. В переписку ключ не попадает.

## Проверка

1. Самотест SQL прямо в редакторе — подпись считается верно.
2. vk-сборка в песочнице с поддельным хостом VK и локальным PostgREST-моком:
   после победы уходит счёт, модалка рисует топ, гость видит таблицу без строки «я».
3. Отказ сервера не ломает игру: модалка показывает «Таблица недоступна».

## Честно про накрутку

Счёт по-прежнему присылает клиент (как и у Яндекса). Подпись защищает от
«я чужой игрок», но не от «накрутил у себя». Лечится только серверной
проверкой слов — это отдельная работа. Пока: рост монотонный, с потолком.

### Проверено (9 сентября)

- SQL целиком применён на одноразовом кластере PostgreSQL 16 (не на боевом
  проекте): самопроверка подписи печатает три «ok», подделанный `vk_user_id`
  отбивается (`bad_sign`), роль `anon` не может прочитать таблицу напрямую
  («permission denied for table leaderboard»), счёт не убывает, второй рывок
  за 20 секунд отбрасывается, за час прирост срезается до +30, `leaderboard_top`
  возвращает форму лидерборда Яндекса с местом игрока.
- Клиент: vk-сборка поднята с моком PostgREST, в запросах видно сырую строку
  запуска и ключ в заголовках, топ рисуется, при 503 модалка показывает
  «Таблица недоступна», а игра остаётся живой. Тёмная и светлая темы сняты.
- `.env.vk.local` с адресом мока удалён, обе сборки (`vk`, `yandex`) пересобраны.

### Сервер поднят (10 сентября)

SQL применён в боевом проекте, ключ приложения на месте: живой
`submit_vk_score` с посторонней подписью отвечает `{"ok": false, "error":
"bad_sign"}`, а `leaderboard_top` из vk-сборки возвращает пустую таблицу
(«Здесь пока пусто», а не «недоступна»). `.env.vk.local` заполнен из
`.env.local`, сборка пересобрана.

Осталось: вписать ID приложения в `vk-hosting-config.json` и выполнить
`npm run deploy:vk`.

---

# Раскладка: клавиатура и поле на разных экранах — план (11 сентября 2026)

## Жалоба

«Адаптив не всё покрывает: бывает негармонично, клавиатура маленькая».

## Замеры до правки (ширина×высота клавиши, клетка поля, px)

| Экран | Клавиша | Клетка | Наблюдение |
|---|---|---|---|
| 320×568 | 23×40 | 48 | тесно, помещается |
| 360×560 | 26×39 | 47 | клавиши низкие |
| 375×667 | 27×47 | 59 | норма |
| 390×844 | 27×52 | 67 | пустота 54 между полем и клавиатурой |
| 412×915 | 29×52 | 67 | пустота 89 |
| 911×700 (VK) | 44×49 | 59 | норма |
| 1440×900 | 44×52 | 88 | поле огромное, клавиши мелкие |
| 768×1024 | 44×52 | 98 | то же, сильнее |
| 740×360 (альбом) | 29×30 | 38 | в VK отключено ориентацией |

## Причины

1. Правило клавиш для мыши (`@media (hover: hover) and (pointer: fine)`,
   строка ~1688) стоит в файле РАНЬШЕ общего `.key` (~2062) при равном весе —
   оно мёртвое. На компьютере клавиши застревают на телефонном потолке 52 px.
2. Поле растёт до 104 px на клетку, клавиши — нет: пропорция разваливается.
3. На высоких телефонах клавиша упирается в 52, лишняя высота уходит в
   пустую полосу.

## Шаги

- [x] Вынести правило клавиш для мыши ниже общего `.key` (или поднять вес),
      чтобы оно работало; потолок высоты клавиши на компьютере — 64 px,
      шрифт растёт вместе с ней
- [x] Потолок клетки на компьютере — 80 px вместо 104: поле не должно
      перерастать клавиатуру
- [x] Телефон: потолок высоты клавиши 60 вместо 52, чтобы высота высоких
      экранов шла в клавиши, а не в пустоту
- [x] Прогнать тот же набор экранов, сравнить таблицу «до/после», снять
      снимки 412×915, 1440×900, 768×1024
- [x] Проверить, что не сломались 320×568 и фрейм VK 911×700

### Замеры после правки

| Экран | Клавиша до → после | Клетка до → после | Пустота над клавиатурой |
|---|---|---|---|
| 320×568 | 23×40 → 23×40 | 48 → 48 | 6 → 6 |
| 360×560 | 26×39 → 26×39 | 47 → 47 | 6 → 6 |
| 375×667 | 27×47 → 27×47 | 59 → 59 | 7 → 7 |
| 390×844 | 27×52 → 27×59 | 67 → 67 | 54 → 44 |
| 412×915 | 29×52 → 29×60 | 67 → 67 | 89 → 77 (симметрично с отступом над полем) |
| 911×700 (VK) | 44×49 → 44×50 | 59 → 58 | 14 |
| 1000×760 (VK) | 44×52 → 44×55 | 65 → 64 | 13 |
| 1280×720 | 44×50 → 44×52 | 61 → 60 | 14 |
| 1440×900 | 44×52 → 44×64 | 88 → 74 | 40 |
| 768×1024 | 44×52 → 44×64 | 98 → 74 | 102 (планшет, симметрично) |

Нигде клавиатура не вылезает за низ. Маленькие и средние телефоны и фрейм VK
не изменились; большие экраны стали пропорциональными. Ширину клавиши на
телефоне задаёт ряд из 12 букв — это физический предел, его правка не трогает.

## Переименование «Буквица» → «Буклица» (12.09.2026)

Причина: на Яндекс.Играх опубликована чужая игра того же жанра с названием
«Буквица» (yandex.ru/games/app/bukvitsa-561364, автор belblch), автопроверка
названий не пропускает наш черновик. Новое имя — отсылка к сове Букле.

- [x] Проверить каталог Яндекса: «Буквица» занята живой игрой, «Буклица» свободна
- [x] Заменить название в коде и текстах (21 упоминание, 14 файлов)
- [x] Плитки с названием в генераторе скриншотов (`marketing/yandex-screens.html`)
- [ ] Логотип и плакат — перерисовывает автор, из них нарезать иконки и сниппет
- [ ] Перевыпустить маркетинговые скриншоты с новым названием
- [ ] Сменить название в кабинетах VK и Яндекса (руками)
- [ ] Домен веб-версии bukvitsa.vercel.app — решить, переезжаем ли

Латиница `buklitsa` менять не потребовалась: архивы сборок и отладочный объект
уже назывались так.
